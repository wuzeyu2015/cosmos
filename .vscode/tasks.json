{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build something",
            "type": "shell",
            "command": "g++",
            "args":[
                "${file}","-o", "${workspaceRoot}\\main.exe", "-g", "-Wall", "-std=c++11"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}